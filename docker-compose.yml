services:
  qube:
    # bqskitrs 0.4.1 has no Linux ARM64 distribution. Use AMD64, which Docker
    # Desktop can emulate on Apple Silicon.
    platform: linux/amd64
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - PORT=8000
